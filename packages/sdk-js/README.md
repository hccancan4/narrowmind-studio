# @narrowmind/client

TypeScript SDK for embedding a NarrowMind DSLM in any website. Will wrap the Ollama HTTP API plus optional retrieval against an exported LanceDB snapshot, and will ship as a dual ESM/CJS build published to npm.

> Status: **placeholder** — to be implemented in [ROADMAP Phase 6](../../docs/ROADMAP.md#phase-6--deployment--integration-35-days).
