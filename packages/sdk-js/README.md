# @narrowmind/client

TypeScript SDK for embedding a NarrowMind DSLM in any website. Wraps the Ollama HTTP API plus optional retrieval against an exported LanceDB snapshot. Dual ESM/CJS build, published to npm.

> Status: **placeholder** — implemented in [ROADMAP Phase 6](../../docs/ROADMAP.md#phase-6--deployment--integration-3-5-days).
